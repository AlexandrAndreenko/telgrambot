name: Test for github runner
on:
  push:
    branches:
      - '**' 
  pull_request:
    branches-ignore:
      - '**'   

jobs:
  test_deploy:
    runs-on: self-hosted
    steps:
    - name: Deploy to test env
      if: github.ref == 'refs/heads/test'
      options: --user 1001
      run: |
        
        whoami
        cd /home/telgrambot
        bash ./testscript.sh Testapp-env

